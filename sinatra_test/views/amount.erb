<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>D3: Czech bar</title>
    <script type="text/javascript" src="../d3/d3.v3.js"></script>
    <style type="text/css">
      }
</style>
  </head>
  <body>
    <script type="text/javascript">

    dataset = @total_amount

      //Width and height
      var w = 1200;
      var h = 800;

      //Create SVG element
      var svg = d3.select("body")
            .append("svg")
            .attr("width", w)
            .attr("height", h);

      svg.selectAll("rect")
         .data(dataset)
         .enter()
         .append("rect")
         .attr("x", function(d, i) {
            return i * (w / dataset.length);
         })
         .attr("y", 0)
         .attr("width", 20)
         .attr("height", 100);

        });
    </script>
<%= "Czech Republic gets a total amount #{@total_amount.to_s("F")} Euro farmsubsidy from the EU" %>
  </body>
</html>
